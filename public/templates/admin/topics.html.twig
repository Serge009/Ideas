{% extends "admin.html.twig" %}
{% block content %}
    <!--
    <table class="table table-striped table-hover">
        <thead>
        <tr>
            <th>Creator</th>
            <th>Date</th>
            <th>Title</th>
            <th>Message</th>
            <th>Picture</th>
            <th>Action</th>
        </tr>
        </thead>
        <tbody>
        {% for topic in topics %}
            <tr>
                <td>{{ topic.creator.name ~ ' ' ~ topic.creator.surname }}</td>
                <td>{{ topic.dateCreated | date('Y-m-d H:i:s') }}</td>
                <td>{{ topic.title }}</td>
                <td>{{ topic.message }}</td>
                <td>{{ topic.picture }}</td>
                <td>
                    {% if topic.deleted %}
                        <a href="{{ url }}topics/activate/{{ topic.id }}">
                            <i class="glyphicon glyphicon-ok"></i>
                        </a>
                    {% else %}
                        <a href="{{ url }}topics/delete/{{ topic.id }}">
                            <i class="glyphicon glyphicon-remove"></i>
                        </a>
                    {% endif %}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

-->


    <div class="panel-group" id="topics-accordion">
    {% for topic in topics %}
        <div class="panel panel-default">

            <a data-toggle="collapse" data-parent="#topics-accordion" href="#collapse-{{ topic.id }}">
                <div class="panel-heading">
                    <div class="topic-title">{{ topic.title }}</div>
                    <div class="topic-author"
                        {% if admin %}
                            style="opacity: 0">1
                        {% else %}
                        >{{ topic.creator.name ~ ' ' ~ topic.creator.surname }}
                        {% endif %}
                    </div>
                    <div class="topic-time">{{ topic.timeAgo }}</div>
                    <div class="topic-comments">
                        {% if admin != true %}
                        <i class="fa fa-comment"></i>
                        {{ topic.activeCommentsCount }}
                        {% endif %}
                    </div>
                    <div class="topic-rate">
                        {% for i in range(1, topic.mark) if i > 0 %}
                            {% if i is even %}
                                <i class="fa fa-star"></i>
                            {% elseif i == topic.mark %}
                                <i class="fa fa-star-half"></i>
                            {% endif %}
                        {% endfor %}
                    </div>

                </div>
            </a>

            <div id="collapse-{{ topic.id }}" class="panel-collapse collapse">
                <div class="panel-body">
                    <div class="topics-header">
                        <p>{{ topic.message }}</p>
                    </div>
                    <div class="topics-embed embed-responsive embed-responsive-16by9"
                         style="{%  if  topic.linkType != 1 %}display: none;{% endif %}">
                        <iframe class="embed-responsive-item"
                                src="{{ topic.picture }}" ></iframe>


                    </div>
                    {% if topic.linkType == 2 %}
                        <div class="topics-embed">
                            <img src="{{ topic.picture }}" />
                        </div>
                    {% endif %}
                    <br />
                    <div class="topics-comments">
                        <table class="table table-striped table-hover">

                            <tbody>
                            {% for comment in topic.comments %}
                                <tr>
                                    <td>{{ comment.creator.name }}</td>
                                    <td>{{ comment.comment }}</td>
                                    <td>{{ comment.dateCreated|date('Y-m-d H:i:s') }}</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>

                </div>
            </div>
        </div>
    {% endfor %}

    </div>

{% endblock %}